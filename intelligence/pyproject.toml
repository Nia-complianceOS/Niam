[project]
name = "niam-intelligence"
version = "0.1.0"
requires-python = ">=3.11"
[tool.setuptools.packages.find]
where = ["."]
include = ["graph*", "ingestion*", "legal*", "retrieval*", "reconciliation*", "reasoning*"]
